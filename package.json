{
  "name": "horde-notify",
  "version": "1.0.0",
  "description": "Slack notification bot for Horde preflight jobs (and eventually more)",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@slack/web-api": "^7.8.0"
  },
  "devDependencies": {
    "@types/node": "^24.12.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}
